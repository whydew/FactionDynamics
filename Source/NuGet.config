<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- This project references RimWorld's own DLLs directly and needs no packages.
       Clearing the sources keeps `dotnet build` fully offline. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
